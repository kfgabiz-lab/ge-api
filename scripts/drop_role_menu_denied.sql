-- ============================================================
-- 역할-메뉴 사이트별 권한 거부 override(role_menu.denied) 제거
-- add_role_menu_denied.sql 로 추가했던 컬럼을 되돌린다.
-- 공통(site_id IS NULL) 권한을 특정 사이트에서만 차단하는 방식을 폐기하고,
-- 사이트 전용 매핑 행의 존재 여부만으로 권한을 판단하는 방식으로 되돌렸기 때문이다.
-- 엔티티에서 필드를 제거해도 ddl-auto=update 는 컬럼을 자동 삭제하지 않으므로
-- 컬럼 자체를 지우려면 이 스크립트를 수동으로 실행해야 한다.
-- 실행 전 반드시 백업 권장
-- ============================================================

ALTER TABLE role_menu
    DROP COLUMN IF EXISTS denied;
